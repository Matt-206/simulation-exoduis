# Simulation Exoduis

Python simulation project for running scenario analyses and exporting results.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
python run.py
```
